apiVersion: apps/v1
kind: Deployment
metadata:
  name: orders-service
  namespace: shophub
spec:
  replicas: 1
  selector:
    matchLabels:
      app: orders-service
  template:
    metadata:
      labels:
        app: orders-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/metrics"
        prometheus.io/port: "8000"
    spec:
      containers:
        - name: app
          image: absolootly/orders-service:latest
          imagePullPolicy: Always

          ports:
            - containerPort: 8000

          env:
            - name: OTEL_SERVICE_NAME
              value: "orders-service"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://monitoring-grafana-alloy.monitoring.svc.cluster.local:4318"
            - name: OTEL_TRACES_SAMPLER_ARG
              value: "1.0"
            - name: DEPLOYMENT_ENV
              value: "production"
            - name: SERVICE_VERSION
              value: "1.0.0"

            - name: PAYMENTS_SERVICE_URL
              value: "http://payments-service"

          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi

          livenessProbe:
            httpGet:
              path: /create-order
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 10

          readinessProbe:
            httpGet:
              path: /create-order
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 5
