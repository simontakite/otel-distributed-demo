---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: orders-service
  namespace: shophub
spec:
  replicas: 1
  selector:
    matchLabels:
      app: orders-service
  template:
    metadata:
      labels:
        app: orders-service
        sidecar.opentelemetry.io/inject: "true"
    spec:
      containers:
        - name: app
          image: absolootly/orders-service:latest
          ports:
            - containerPort: 8000
          resources:
            requests:
              memory: "128Mi"
              cpu: "250m"
            limits:
              memory: "256Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: orders-service
  namespace: shophub
spec:
  selector:
    app: orders-service
  ports:
    - port: 80
      targetPort: 8000